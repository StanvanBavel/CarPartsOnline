name: Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 'ui-carpartsonline'
    strategy:
      matrix:
        node-version: [14.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
    - name : Cache
      uses: actions/cache@v2
      with:
        path: "frontend/node_modules"
        key: node-modules-${{ hashFiles('ui-carpartsonline/package.json') }}
    - run: npm ci
      working-directory: ./ui-carpartsonline
    - run: npm run build --if-present
      working-directory: ./ui-carpartsonline
    
#   docker:
#     name: Publish - Docker Hub
#     runs-on: ubuntu-18.04
#     needs: [build]
#     env:
#       REPO: ${{ secrets.DOCKER_REPO }}
#     steps:
#       - uses: actions/checkout@v1
#       - name: Set up JDK 11
#         uses: actions/setup-java@v1
#         with: 
#           java-version: 11.0.4
# #       - name: Login to Docker Hub
# #         run: docker login -u ${{ sercrets.DOCKER_USER }}
# #              -p ${{ secrets.DOCKER_PASS }}
#       - name: Build Docker Image
#         run: docker build -t $REPO:latest -t $REPO:${GITHUB_SHA::8}
#       - name: Publish Docker Image
#         run: docker push $REPO
     
  
  build-and-deploy-front-end:
    name: Build and Deploy Front-end
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v1
      with:
        username: ${{ secrets.DOCKER_USEERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        repository: StanvanBavel/CarPartsOnline/tree/main/ui-carpartsonline
        dockerfile: ui-carpartsonline/Dockerfile
        tags: latest

# This workflow will do a clean install of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

# name: Node.js CI

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   build:

#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         node-version: [14.x]
#         # See supported Node.js release schedule at https://nodejs.org/en/about/releases/
    

      
#     steps:
#     - uses: actions/checkout@v2
#     - name: Use Node.js ${{ matrix.node-version }}
#       uses: actions/setup-node@v2
#       with:
#         node-version: ${{ matrix.node-version }}
#         cache: 'npm'
#         #cache-dependency-path: CarPartsOnline/ui-carpartsonline/
#         cache-dependency-path: 'ui-carpartsonline//package.json'
        
  
#     - run: npm ci
#     - run: npm run build --if-present


